import { getColor } from '../../../helpers/get-color';
import { module, test } from 'qunit';

module('Unit | Helper | get color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getColor([42]);
  assert.ok(result);
});
